@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    Layout = "_Layout";
}
@using Microsoft.AspNetCore.Http;
@model School.Models.Student

@if(Model is null){
    <p>not found user</p>
}
<h1>Add New Student To The Portal</h1>
<form method="post" asp-action="Edit" asp-controller="Student">
    <div class="mt-3">
        <label class="form-label">Id</label>
        <input type="text" class="form-control" asp-for="Id" readonly />
        <span asp-validation-for="Name"></span>
    </div>
    <div class="mt-3">
        <label class="form-label">Name</label>
        <input type="text" class="form-control" asp-for="Name" />
        <span asp-validation-for="Name"></span>
    </div>

    <div class="mt-3">
        <label class="form-label">Email</label>
        <input type="text" class="form-control" asp-for=" Email" />
        <span asp-validation-for="Email"></span>

    </div>

    <div class="mt-3">
        <label class="form-label">Phone</label>
        <input type="text" class="form-control" asp-for="PhoneNumber" />
        <span asp-validation-for="PhoneNumber"></span>

    </div>
    <div>
        <label asp-for="governorate"></label>
        <select asp-for="governorate" class="field" asp-items="@Html.GetEnumSelectList(typeof(Governorate))">
            <option value=""></option> select
        </select>
        <span asp-validation-for="governorate"></span>
    </div>
    <div class="mt-3">
        <button type="submit" class="btn btn-primary">Save</button>
        @* @{ *@
        @*     string role = ViewData["role"].ToString();   *@
        @* } *@
       @* @if(role!.Equals("Admin")) *@
       
       <button type="submit" class="btn btn-danger" asp-action="Delete" asp-controller="Student" onclick="return confirm('Are you sure you want to delete this student?');">Remove</button>
       
       </div>

</form>
